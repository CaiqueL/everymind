import { Outlet } from "react-router-dom";
import { BigCard } from "../../../components/BigCard";

const Feedbacks = () => {
    return (
      <div>
        <BigCard/>
      </div>  
    )
};

export default Feedbacks;
