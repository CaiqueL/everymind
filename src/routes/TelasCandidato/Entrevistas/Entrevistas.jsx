import { Outlet } from "react-router-dom";
import { BigCard } from "../../../components/BigCard";

const Entrevistas = () => {
    return (
      <div>
        <BigCard/>
      </div>  
    )
};

export default Entrevistas;
